services:
  web:
    image: ghcr.io/basecamp/fizzy:main
    restart: unless-stopped
    ports:
      - "4991:80"
    volumes:
      - ${APP_DATA_DIR}/:/rails/storage
    environment:
      - SECRET_KEY_BASE=umbrel4life
      - DISABLE_SSL=true
      - BASE_URL=http://umbrel.local:4991