services:
    goaway:
        image: 'pommee/goaway:latest'
        ports:
            - '7417:8080'
            - '53:53/tcp'
            - '53:53/udp'
        container_name: goaway
        restart_policy: always
        cap_add:
            - NET_BIND_SERVICE
            - NET_RAW    
    
